[package]
name = "guard-test-probe"
version.workspace = true
edition.workspace = true
license.workspace = true

# No IP network code. The only transport is an AF_UNIX synthetic-canary sink.
[dependencies]
libc = { workspace = true }
rusqlite = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
