# macOS 阶段 37：文档清理与中文化收口

## BASE HEAD

`c74db43`

## 清理范围

- 删除旧版 macOS 开发、发布、启用教程和重复的中文打包入口。
- 删除早期 Linux 阶段报告、旧 macOS 阶段报告和重复 ADR；它们不再代表当前实现，避免被用户误用。
- 删除原英文技术说明，保留并重写为中文的当前有效说明：平台架构、macOS Endpoint Security、安全模型、SSH 访问模型。
- 统一安装文档文件名为中文，并更新 README、AUR、服务注释和测试脚本中的链接。
- 保留当前有效的 macOS 阶段 34–36 报告，作为最近 UI、脚本和文档入口的验收证据。

## 当前文档白名单

当前用户文档仅保留根 README、`docs/` 下的中文主线、许可证说明、测试夹具说明和最近 macOS 报告。`AGENTS.md` 是仓库工程约束，不属于用户教程，保留原文件名供自动化执行规则使用。

## 检查

通过：

```text
git diff --check
旧文档名和旧路径全文检索无结果
Markdown 白名单无旧英文安装/历史报告文件
```

## FINAL STATUS

仓库不再保留旧教程、旧 ADR 或早期英文阶段报告。用户只需从根 README 或 `docs/README.md` 进入当前中文文档主线。
